[pytest]
testpaths = tests
markers =
    ai_workflow: AI workflow, OpenCode, task orchestration, and worktree contract tests
    game_logic: game runtime, state machine, vision, OCR, and gameplay behavior tests
